import React from 'react';
import styles from '../styles/NotFound.module.css';

const NotFound = () => {
    return (
        <div className={styles.centerDiv}>
            <h1 className={styles.bigFont}>404 Error</h1>
        </div>
    );
};

export default NotFound;
